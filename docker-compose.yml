services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fish_study_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
